<div id="<%= dom_id blog %>">
  <p>
    <strong>Title:</strong>
    <%= link_to blog.title, blog_path(blog) %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= blog.body %>
  </p>

  <small>
    Published
    <%= distance_of_time_in_words(blog.created_at, Time.now) %>
    ago
  </small>
</div>
